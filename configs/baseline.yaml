exp_name: baseline
seed: 42

dataset:
  name: cifar10
  root: ./data
  num_classes: 10

dataloader:
  batch_size: 64
  num_workers: 2

model:
  name: resnet18
  num_classes: 10

# training:
#   epochs: 10
#   device: auto  

# optimizer:
#   name: sgd
#   lr: 0.01
#   momentum: 0.9
#   weight_decay: 0.0005

scheduler:
  name: none

# logging:
#   save_best: true
#   save_last: true