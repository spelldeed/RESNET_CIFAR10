# ResNet on CIFAR-10: Reproduction and Experiments

## Objective
Reproduce a ResNet baseline on CIFAR-10 and run controlled experiments.

## Project Structure
- configs/ : experiment configs
- src/ : core training and model code
- scripts/ : run scripts
- results/ : outputs (ignored by git)
- notebooks/ : analysis / Colab notebooks

## Setup (Colab)
Install dependencies:
```python
!pip install -r requirements.txt